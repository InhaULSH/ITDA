# YelpZip EDA 보고서

## 1. 분석 목적

이 보고서는 `data/origin/yelpzip.csv`의 탐색적 분석 결과가 최종 GNN 모델 설계에 어떻게 연결되었는지를 정리한다. 현재 프로젝트에서 유지하는 기준 모델은 `topk15 window60 SAGE`이며, 방향성 피처 적용 모델은 같은 샘플과 같은 엣지 위에서 노드 피처만 확장한 비교 후보이다. 따라서 이 문서의 EDA 해석은 우선 기존 `topk15 window60 SAGE` 모델을 기준으로 설명한다.

리뷰 어뷰징은 단순히 짧은 문장이나 낮은 별점 하나로 정의하기 어렵다. 실제 플랫폼에서는 정상 사용자도 짧은 리뷰를 쓸 수 있고, 정상 고객도 강한 불만 때문에 1점을 줄 수 있다. 그러므로 EDA의 핵심 질문은 “가짜 리뷰가 어떤 단일 특징을 갖는가”가 아니라 “평판을 움직일 유인이 있는 상황에서 어떤 행동 단서들이 함께 나타나는가”이다.

이 관점에서 최종 모델은 리뷰 하나를 하나의 노드로 두고, 리뷰 자체의 특성, 사용자 과거 맥락, 상품 과거 맥락, 텍스트 표현, 그리고 리뷰 간 관계를 함께 사용한다. 라벨, 원본 `tag`, 전체 기간 fake rate, 미래 리뷰 수처럼 예측 시점에 알 수 없거나 정답을 직접 반영하는 정보는 모델 입력에서 제외한다.

## 2. 데이터 기본 구조

YelpZip 원본 데이터는 리뷰 608,458건으로 구성된다. 사용자는 260,239명, 상품 또는 식당은 5,044개이며, 날짜 범위는 2004-10-20부터 2015-01-10까지이다. 원본 라벨은 `label=-1`이 fake, `label=1`이 real에 대응되며, 모델 학습에서는 이를 `is_fake=1`, `is_fake=0`으로 변환한다.

전체 fake 리뷰는 80,439건으로 전체의 약 13.22%이다. 정상 리뷰가 훨씬 많기 때문에 단순 정확도만 높이는 모델은 의미가 약하다. 실제 운영에서는 정상 리뷰를 대량으로 오탐하지 않으면서도, 어뷰징 가능성이 높은 리뷰를 검토 큐 상단으로 올리는 능력이 중요하다. 그래서 최종 평가는 PR-AUC, ROC-AUC, Macro F1, Precision, Recall을 함께 본다.

## 3. 평점 신호

Yelp 리뷰는 전체적으로 4점과 5점이 많은 긍정 편향을 가진다. 따라서 높은 평점 자체는 어뷰징 신호로 충분하지 않다. 그러나 fake rate를 평점별로 나누면 극단 평점에서 위험도가 높아진다. 특히 1점 리뷰의 fake rate가 가장 높고, 5점 리뷰도 전체 평균보다 높다.

이 결과는 평판 조작의 경제적 유인과 맞다. 긍정 조작은 상품의 인상을 끌어올리기 위해 높은 별점을 사용하기 쉽고, 부정 공격은 경쟁 상품의 평판을 떨어뜨리기 위해 낮은 별점을 사용하기 쉽다. 다만 최종 `topk15 window60 SAGE`에서는 긍정 조작과 부정 공격을 별도 피처로 직접 분리하지 않고, `rating_direction`, `extreme_rating`, `rating_deviation_from_prior_product_mean`, `standardized_rating_deviation_from_prior_product_mean`처럼 평점 방향과 상품 과거 평균 대비 이탈을 일반적인 형태로 반영한다. 방향성 피처 적용 후보에서는 이 해석을 더 명시적으로 분리한다.

## 4. 텍스트 길이와 설명 충분성

EDA에서 정상 리뷰는 fake 리뷰보다 평균적으로 길었다. 짧은 리뷰 구간의 fake rate도 전체 평균보다 높았다. 이는 실제 경험을 설명하는 데 드는 비용과 관련된다. 실제 방문 경험이 있으면 음식, 서비스, 가격, 분위기, 상황을 구체적으로 적기 쉽지만, 조작 리뷰는 평판 신호를 만드는 것이 목적이므로 짧고 단정적인 문장으로도 목적을 달성할 수 있다.

하지만 “짧은 리뷰는 가짜”라는 규칙은 위험하다. 정상 사용자도 “좋았어요”, “다시는 안 갑니다”처럼 짧게 쓸 수 있다. 특히 평점대마다 자연스러운 리뷰 길이가 다르고, 리뷰 이력이 많은 상품과 적은 상품에서도 문장 길이 분포가 다를 수 있다. 그래서 최종 모델은 단순 `word_len`만 보지 않고, 같은 평점대와 상품 이력 구간 안에서 상대적으로 짧은지를 보는 피처를 추가했다. 구체적으로 train 구간에서 평점 bucket과 상품 이력 구간별 단어 수 분위수를 계산하고, 각 리뷰가 그 기준보다 짧은지 또는 충분한 설명을 갖는지 표현한다.

이 방식은 정보 누수를 줄이기 위해 train split 기준으로만 기준값을 fit하고, validation/test에는 같은 기준을 적용한다.

## 5. 사용자 맥락

EDA에서는 신규 사용자와 단발성 사용자 맥락이 중요했다. test 구간에서 train에 등장한 적 없는 사용자의 리뷰는 상대적으로 fake rate가 높았고, 기존 사용자의 리뷰는 상대적으로 안정적이었다. 그러나 같은 사용자가 여러 번 리뷰를 남겼다는 사실 자체를 위험 신호로 보면 안 된다. 실제 활성 사용자도 반복해서 리뷰를 작성하기 때문이다.

따라서 최종 모델은 사용자 ID를 숫자 피처로 직접 넣지 않는다. 대신 작성 시점 이전에 알 수 있는 사용자 과거 리뷰 수, 마지막 리뷰 이후 경과일, 과거 평균 평점, 과거 극단 평점 비율 같은 요약 맥락을 사용한다. R-U-R 엣지도 “같은 사용자는 위험하다”는 의미가 아니라, 같은 사용자의 다른 리뷰 흐름을 통해 정상적인 활동 습관 또는 비정상적인 반복 행동을 주변 맥락으로 전달하는 장치로 사용한다.

## 6. 상품 맥락과 평판 조작 유인

리뷰 조작은 평판을 움직일 수 있는 상황에서 더 큰 의미를 가진다. 리뷰 이력이 적은 상품은 하나의 리뷰가 평균 평점에 주는 영향이 크고, 리뷰 이력이 많은 상품은 갑작스러운 부정 리뷰가 평판 방어 측면에서 중요할 수 있다. 최종 `topk15 window60 SAGE`는 이 아이디어를 `prior_product_review_count`, `prior_product_avg_rating`, `prior_product_rating_std`, `rating_impact_abs`, `weak_product_prior_count_flag` 같은 피처로 반영한다.

특히 중요한 것은 평점 이탈을 절댓값으로만 보지 않는 것이다. 어떤 상품은 원래 평점 변동이 크고, 어떤 상품은 매우 안정적이다. 같은 1점 차이라도 안정적인 상품에서는 큰 사건일 수 있고, 변동성이 큰 상품에서는 흔한 변동일 수 있다. 그래서 샘플링 flag와 shock 엣지에서는 과거 상품 평점 표준편차를 기준으로 표준화한 평점 이탈을 사용한다.

## 7. 시간 집중과 상품-주 단위

전체 월별 리뷰 수나 요일별 fake rate만으로는 어뷰징을 충분히 설명하기 어렵다. 더 중요한 것은 특정 상품의 특정 주간에 리뷰가 몰리고, 그 리뷰들이 평점 방향이나 행동 특성에서 평소와 다르게 움직이는지이다. 그래서 최종 샘플링 단위는 리뷰 개별 행이 아니라 `prod_id × week`이다.

단순히 리뷰 수가 많은 상품-주를 모두 가져오면 정상 인기 상승도 함께 들어온다. 반대로 너무 강한 의심 조건만 걸면 모델이 정상 리뷰의 다양한 분포를 잃는다. 최종 샘플링은 `n_reviews >= 10`, `n_users >= 8`을 기본 gate로 두고, 그 안에서 여러 행동 flag 중 2개 이상을 만족하는 상품-주를 선택한다. 이때 flag는 가능한 한 과거 흐름 대비 변화량을 사용해, 전체 분포를 지나치게 좁히지 않으면서도 의미 있는 분석 대상을 선택하도록 설계했다.

## 8. 템플릿 반복

정확히 같은 텍스트가 반복되는 경우는 강한 단서지만, 실제 데이터에서는 매우 희소하다. 정확 중복만 보면 템플릿을 조금씩 바꾼 조작을 놓칠 수 있고, 반대로 중복 신호 하나에 과하게 의존하면 모델이 일반화하기 어렵다. 최종 기준 모델에서는 기존 숫자 피처와 텍스트 TF-IDF/SVD 임베딩을 유지하고, 방향성 피처 후보에서는 상품-주 안에서 약하게 비슷한 텍스트 패턴을 피처와 설명 산출물에 더 명확히 반영한다.

## 9. 최종 모델 설계로 연결된 EDA 결론

EDA에서 최종 모델로 직접 연결된 결론은 다음과 같다.

첫째, 리뷰 어뷰징은 극단 평점, 짧은 설명, 신규 사용자, 약한 상품 이력, 평점 방향 집중이 단독으로 나타나는 사건이 아니라 함께 나타날 때 의미가 커진다. 따라서 샘플링은 하나의 flag가 아니라 여러 flag의 조합을 사용한다.

둘째, 상품별 과거 변동성이 다르므로 평점 shock은 절대 평점 차이보다 과거 변동 수준 대비 이탈로 보는 편이 타당하다. 최종 shock 엣지는 `standardized_abs_rating_deviation >= 0.75`를 사용한다.

셋째, 같은 사용자 relation은 위험 전파용이라기보다 사용자 맥락 전달용이다. 그래서 최종 모델은 R-U-R relation을 유지하되, 같은 상품-주 버스트 relation은 사용하지 않는다.

넷째, 텍스트는 단순 길이보다 “평점과 상품 이력 맥락에 비해 설명이 충분한가”로 보는 편이 안정적이다. 이 기준은 기존 topk15 window60 모델의 보조 피처와 방향성 피처 적용 모델의 설명 피처에 반영된다.

다섯째, 최종 보존 모델은 두 개다. 기존 `topk15 window60 SAGE`는 성능 기준의 1차 후보이고, 방향성 피처 적용 `topk15 window60 SAGE`는 긍정 조작과 부정 공격의 비대칭성, 정상 사용자 보호 맥락을 더 설명하기 쉬운 후보이다.

## 참고 자료

- Michael Luca and Georgios Zervas, “Fake It Till You Make It: Reputation, Competition, and Yelp Review Fraud,” Management Science, 2016.
- Dina Mayzlin, Yaniv Dover, and Judith Chevalier, “Promotional Reviews: An Empirical Investigation of Online Review Manipulation,” American Economic Review, 2014.
- Shebuti Rayana and Leman Akoglu, “Collective Opinion Spam Detection: Bridging Review Networks and Metadata,” KDD, 2015.
- Yelp Trust & Safety, Consumer Alerts.
- Amazon, review authenticity and fake review detection policy materials.
